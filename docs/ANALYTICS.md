# OpenDeepSeek 数据分析

## 📈 系统状态

### 访问统计
```
┌─────────────────────────────────────────────┐
│           日均响应时间分布                   │
├─────────────────────────────────────────────┤
│  < 50ms    ████████████████████  60%        │
│  50-100ms  ██████████           30%        │
│  > 100ms   ████                 10%        │
└─────────────────────────────────────────────┘
```

### 服务健康状态
```
┌─────────────────────────────────────────────┐
│  服务              状态    内存     CPU     │
├─────────────────────────────────────────────┤
│  Open WebUI      ✅     512MB    0.5     │
│  Hermes Bridge  ✅     128MB    0.3     │
│  Hermes Agent   ✅     768MB    1.2     │
│  Nginx        ✅     12MB     0.1     │
│  Tailscale    ✅     8MB      0.05    │
└─────────────────────────────────────────────┘
```

## 🤖 模型分析

### 可用模型对比
```
┌────────────────────────────────────────────────────────────┐
│                    GPT-5 模型系列                        │
├──────────────┬────────┬────────┬────────┬────────────────┤
│ 模型        │ 推理   │ 速度   │ 代码   │ 成本         │
├──────────────┼────────┼────────┼────────┼────────────────┤
│ gpt-5.5     │ ★★★★★ │ ★★★   │ ★★★★★ │ $$$           │
│ gpt-5.4     │ ★★★★  │ ★★★★  │ ★★★★  │ $$            │
│ gpt-5.4-mini│ ★★    │ ★★★★★ │ ★★    │ $             │
└──────────────┴────────┴────────┴────────┴────────────────┘
```

### 选择建议
```
场景                    推荐模型
─────────────────────────────────────────
复杂代码/推理        →  gpt-5.5 ⭐
日常对话/写作      →  gpt-5.4
快速简单查询      →  gpt-5.4-mini
```

## 🔄 流量架构

### 请求流向图
```
用户请求
   │
   ├──→ Open WebUI ──→ Hermes Bridge ──→ GPT-5 API
   │                                           │
   │  简单问答     │                    │
   │              └──────────────────────────→ 直接返回
   │                                                  
   │  Agent任务                                         
   │              ↓                             
   │         Hermes Agent ──→ 工具执行 ──→ 文件/终端/网页
   │                         ↓
   └─────────────────────────���───→ 返回结果
```

## 📊 性能指标

### 响应时间 (ms)
```
API测试:
• gpt-5.5: 2450ms (推理完整)
• gpt-5.4: 1800ms (平衡模式)  
• gpt-5.4-mini: 1200ms (快速模式)

网络延迟:
• Tailscale内网: ~31ms
• 本地Docker: ~10ms
```

### 成本估算 (每百万tokens)
```
模型          输入        输出
─────────────────────────────────
gpt-5.5     $5.00      $30.00
gpt-5.4     $2.50      $15.00
gpt-5.4-mini $0.75      $4.50
```

## 🎯 使用建议

1. **日常对话** → gpt-5.4-mini (快、省)
2. **代码任务** → gpt-5.5 (最强)
3. **复杂推理** → gpt-5.5 (完整思考)

## 📋 快速检查命令

```bash
# 检查服务状态
docker ps

# 查看实时日志
docker logs -f opendeepseek-hermes

# 测试API响应
curl -X POST https://cli.1001001.best/v1/chat/completions \
  -H "Authorization: Bearer sk-IgxaJiFOLWbopPP5i" \
  -H "Content-Type: application/json" \
  -d '{"model": "gpt-5.5", "messages": [{"role": "user", "content": "test"}], "max_tokens": 10}'

# 检查网络
ping 100.103.214.38

# 查看证书
openssl s_client -connect me-virtual-machine.tailcf23f0.ts.net:443 -servername me-virtual-machine.tailcf23f0.ts.net 2>/dev/null | openssl x509 -noout -subject -issuer
```